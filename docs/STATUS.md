# Implementation status

> Historical ML development log, preserved from the feature branch. Statements below about pending teammate integration and bilingual READMEs describe that earlier stage. The unified project's current setup is ../README.md; current acceptance evidence is VERIFICATION.md. Selected weights are unchanged by the merge.

The branch contains reproducible archive download, explicit-timezone dataset assembly, a PyTorch MLP training module, portable NumPy inference, weather-provider HTTP integration, and a local NVIDIA Brev job controller.

62 local regression checks pass. A real Brev H200 training run for turbine 2 completed after 35 epochs, selecting epoch 25. Downloaded model `mlp-4163decdcbe5` reproduces all 1,478 exported January predictions (739 distinct hours); MAE is 0.18600 versus 0.25300 for the empirical wind curve. The 24- and 48-hour API paths were verified with real archived weather supplied through a simulated HTTP provider. Results remain provisional because measurement interval alignment is unconfirmed. Raw measurements and credentials are not committed. The selected joint weights are now committed under models/production/ for integration.

The automatic controller submits or resumes training, monitors completion, verifies downloaded results and optionally stops the selected GPU. The launcher recorded a successful GPU stop after local recovery of a ZIP-layout bug, now fixed. It can now optionally start an existing stopped instance without provisioning a new GPU; the detached restart path still needs live verification from a stopped instance (the successful joint run found remote GPU access already available). A startup timeout and legacy empty-lock failure were observed for the joint run. The controller now uses an OS-released guard lock, checks old WSL controllers before retiring legacy locks, starts detached and bounds CLI waits; 5 additional regression checks cover lock ownership, crash recovery, process matching, timeout diagnostics and startup recovery. The user subsequently completed the joint run through this controller, including result download and recorded GPU stop. This desktop agent session cannot directly access the authenticated WSL environment, so the user launches a Windows wrapper once.

Both real turbine CSVs are now available. The joint assembly contains 96,900 predictor/target rows for 48,450 turbine-hour targets: 23,666 hours for turbine 1 and 24,784 for turbine 2, each represented at two archive offsets. Both source CSVs end January 31, 2026; no February measurements were supplied. Separate coordinate requests and turbine-ID joins are preserved even though all overlapping JMA weather values are identical at the nearby locations. Missing/ambiguous-row counts and hashes are in `reports/dataset_assembly_both.json`.

The shared network learns turbine-ID features. Its export check covers every trained turbine, and evaluation reports separate metrics and baselines per turbine and offset. A tiny two-turbine fixture verifies this path locally. The joint real-data model `mlp-76540e5972a6` has now completed CUDA training (28 epochs, best checkpoint 18) and supports both turbines. All 2,956 January export predictions were independently reproduced on the CPU (maximum difference 1.82e-7). Test MAE is 0.17390 for turbine 1 and 0.18315 for turbine 2; combined MAE 0.17853 is 29.1% below the wind-curve baseline. Both real-model 48-hour direct API paths passed in-process checks using archived weather. The launcher saved results and recorded a successful GPU stop. See `reports/cloud_training_both_verification.json`.

The `yevgeniy` branch was inspected read-only. `docs/TEAM_INTEGRATION_PROPOSAL.md` proposes JMA GSM 10 m weather and Django posting prepared rows to a pure inference endpoint. The direct-input /v1/predict adapter and /v1/metadata capability endpoint are now implemented and tested. docs/ML_SERVICE_CONTRACT.md defines the exact fields, authentication and errors. The other party still needs to adapt and perform a live integration test. No other branch was modified.

Remaining: deploy the joint artifact for CPU inference, confirm measurement alignment, perform a live integration test with the other developer, and reproduce competition decisions using original archived runs with verified availability. Fixed-offset training is not proof of exact historical forecast availability.

A fixed model comparison was executed on the real joint dataset with two local CPU threads. Nine new configurations plus the current model and two fixed ensembles were evaluated. The validation-selected average of two MAE-trained networks reduced January MAE from 0.178525 to 0.166403 (6.8%) but increased RMSE from 0.238635 to 0.245725 (3.0%). The larger MAE network had January MAE 0.163725 but was not the validation-selected winner. Current API artifacts remain unchanged. Full results and methodology are in `docs/MODEL_COMPARISON.md` and `reports/model_comparison_v1.json`; `scripts/benchmark_models.py` reproduces the experiment. All 62 regression checks pass, including the real committed release.

Integration release now includes selected weights and provenance in models/production/, a CPU launcher and HTTP smoke test in inference/, a separate training_toolkit entry point, bilingual full READMEs, pinned runtime dependencies, and Docker/Compose configuration. See reports/release_verification.json for the fresh runtime-only HTTP check. Docker engine and teammate integration are not verified here.
